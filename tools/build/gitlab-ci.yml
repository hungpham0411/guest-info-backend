build:
  stage: build
  needs: []
  before_script:
    - apk add bash git
  script:
    - tools/build/commands/build.sh
    - tools/build/commands/run.sh
  artifacts:
    paths:
      - build/openapi.yaml
